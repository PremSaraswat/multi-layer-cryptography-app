# Runtime dependencies: none.
#
# This application uses only the Python standard library (tkinter ships with
# CPython). On Debian/Ubuntu, tkinter is packaged separately:
#     sudo apt install python3-tk
